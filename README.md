# React Steps Project

A simple React app to practice **State Management** and **Event Handling**.

## Key Learnings

- **useState Hook:** Managed component data and triggered re-renders.
- **Event Handling:** Learned to pass function references (`onClick={handler}`) instead of calling them immediately.
- **Functional Updates:** Updated state safely using the callback pattern: `setStep((s) => s + 1)`.
